# Keep sample specs at LF so they stay byte-identical to the upstream files they were fetched
# from; without this, autocrlf on Windows rewrites them and every hash or diff reports noise.
samples/**/*.yml text eol=lf
samples/**/*.yaml text eol=lf
samples/**/*.json text eol=lf
