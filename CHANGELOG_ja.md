# 変更履歴

*This document in [English](CHANGELOG.md).*

このプロジェクトは [セマンティックバージョニング](https://semver.org/lang/ja/) に従います。
1.0.0 までは、プレビュー版の間で公開 API が変わることがあります。

## [未リリース]

まだありません。

## [0.1.0-preview]

最初のプレビューです。以下はすべて動作し、テストで確認していますが、NuGet への公開はまだで、
API も安定していません。

### spec の公開

- OpenAPI 3.0 / 3.1 の定義を、JSON・YAML のどちらでも、ローカルパスまたは URL から読み込む。
- stdio 越しに MCP ツールとして公開する(`mcpsense mcp`)。Claude Desktop や Claude Code が期待する形式。
- 1 オペレーション = 1 ツール。path・query・header・cookie のパラメータはトップレベルの引数になり、
  リクエストボディは `body` という 1 つの引数で渡す。
- ローカルの `$ref` は各ツールのスキーマへインライン展開する。自己参照するスキーマを含む spec でも
  同様で、クライアントが spec を持つ必要はない。
- 上流のエラー応答はそのまま通す。モデルが次の行動を判断するには、API 自身のメッセージが最も役立つため。

### 大きな spec への対応

- 設定可能な閾値(`--threshold`、既定 30)を超えると、1 オペレーション 1 ツールの代わりに 3 つのツール
  — `search_operations` / `describe_operation` / `call_operation` — を公開し、spec が持つタグを目次として渡す。
  `--mode` でどちらかに固定できる。
- 検索は AI 未設定でも動く。既定のインデックスは識別子を単語に分割し、希少な語を一般的な語より重く、
  名前での一致を説明文での一致より上位に扱う。
- `--tag` / `--allow` / `--deny` で公開範囲を絞れる(ワイルドカード可、deny が優先)。

### オプトインの AI 機能

- 任意の `IChatClient` でツール名・説明・パラメータの補足を書き換える(`--ai-model`)。
  リクエスト数を抑えるためバッチ処理する。
- 応答はディスクにキャッシュするため、spec が変わらなければ 2 回目の起動でモデルへの送信は発生しない。
- 任意の `IEmbeddingGenerator` による意味検索(`--ai-embedding-model`)。
  起動時に 1 回のバッチ呼び出しで全オペレーションを埋め込む。
- モデルのエラー・タイムアウト・解釈できない応答はいずれも致命的ではない。
  該当するオペレーションは spec の原文を保ち、サーバーは通常どおり起動する。

### 認証

- 静的な認証情報: `--bearer-token`、`--api-key-header` / `--api-key`、および `--header` による任意のヘッダー。
  ヘッダー無しのリクエストを拒否する API があるため、上書きしない限り `User-Agent` を送る。
- PKCE 付き OAuth 2.0 認可コードフロー(`mcpsense login`)。ループバックリダイレクト、`state` 検証、
  以降のトークン自動更新に対応。更新処理は直列化してあり、同時実行のツール呼び出しが
  同じリフレッシュトークンを二重に使うことはない。
- トークンは提供元・クライアント・スコープごとに保存する。Windows では DPAPI で暗号化し、
  それ以外の OS では所有者のみ読み書き可能な権限で書き込む。
- 認証情報はツールのスキーマにも引数にも現れない。サーバー側で、送信の直前に付与する。

### 意図的に見送ったもの

- **Streamable HTTP。** この通信方式は、サーバーを公開できて初めて意味があります。安全に公開するには
  「誰の接続を受け入れるか」に答える必要があり、さらに「接続したユーザーの呼び出しを誰の権限で行うか」
  という問いも残ります。動くけれど公開してはいけない通信方式は罠になるため、認証とセットになるまで見送りました。
- **ライブラリのパッケージ公開。** 公開するのは `McpSense.Tool` だけです。このツールは `McpSense.Core` /
  `McpSense.Ai` / `McpSense.Server` を自身に同梱し、NuGet 依存を 1 つも宣言しないため、CLI の利用に
  他のパッケージは要りません。個別に公開すると、まだ誰も求めていない公開 API を約束することになります。
  後からパック対象に戻すのは容易ですが、公開したパッケージの取り下げはできません。
- NuGet へは未公開。ソースからインストールしてください。

[未リリース]: https://github.com/pierre3/mcp-sense/compare/v0.1.0-preview...HEAD
[0.1.0-preview]: https://github.com/pierre3/mcp-sense/releases/tag/v0.1.0-preview
