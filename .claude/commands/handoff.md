---
description: 現在のセッション状態を docs/SESSION-HANDOFF.md に保存（次セッションへの引継ぎ）
---

`docs/SESSION-HANDOFF.md` の `<!-- HANDOFF:BEGIN -->` 〜 `<!-- HANDOFF:END -->` の間を、現在のセッションの引継ぎ状態で上書きしてください。区切り線より上の説明とマーカー自体は変更しないこと。

**ファイルが存在しない場合（新規クローン等）は、まず下記の標準テンプレートで新規作成してから書き込む**（このファイルは `.gitignore` 済みのローカル専用）:

```markdown
# SESSION-HANDOFF — セッション引継ぎメモ

> このファイルは**特定セッション時点の一時的な文脈**を次セッションへ渡すためのもの。CLAUDE.md から `@docs/SESSION-HANDOFF.md` で自動読み込みされる。Git 追跡対象外のローカル専用。
> - 恒久的な事実（設計方針・実仕様・規約）は **CLAUDE.md** へ。ここには書かない。
> - 保存は `/handoff`、引継ぎを消化したら `/handoff-clear` で下の「空テンプレート」状態へ戻す。
> - 下の区切り線より上（この説明）は消さない。区切り線より下だけを書き換える。

---

<!-- HANDOFF:BEGIN -->
_（引継ぎ事項なし。作業中の一時状態が生じたら `/handoff` で記録する。）_
<!-- HANDOFF:END -->
```

書く内容（**一時的な文脈のみ**。恒久情報は CLAUDE.md 側なのでここには書かない）:

- **作業中のこと / 中断点:** いま何を途中まで進めたか、次に再開する具体的な一歩。
- **未確定の判断:** 保留中の決定、人の go/no-go 待ち、選択肢が残っている点。
- **直近の状態スナップショット:** 必要なら現時点のビルド/テスト結果や検証済み事項（as-of の値であることを明記）。
- **このセッション固有の環境メモ:** 一時的に効いている設定・回避策など。
- **未コミットの変更:** ある場合はファイルと意図。

ルール:
- git 履歴・CLAUDE.md・コードから辿れる情報は書かない（重複させない）。
- 相対日付は絶対日付に変換する。
- 引継ぎが空なら、無理に埋めず空テンプレート（`_（引継ぎ事項なし。…）_`）のままにする。
- 書き換え後、保存した要点を 2〜4 行でユーザーに報告する。
